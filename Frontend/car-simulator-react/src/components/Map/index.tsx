import { Box } from "@mui/material";

interface IProps {}

export default function Map(props: IProps) {
  return (
    <Box
      sx={{
        height: "100%",
        background: "#fff",
      }}
    >
      <h1>fsdf</h1>
    </Box>
  );
}
